<?php

/**
 * The file /app/config.php should not be kept in source control, as it likes
 * contains settings specific to the environment that the site is running on.
 * This file should contain a boilerplate of the contents of that file, with
 * any environment settings that an application will need to configure such
 * as the MySQL connection details or any api credentials.
 */
